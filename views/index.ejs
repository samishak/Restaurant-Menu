<!DOCTYPE html>
<html>

<head>
  <title>Menu</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Farrouj .. 🐔</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken wrap" name="choice">🍗 Chicken Wrap</button>
    <button type="submit" value="beef wrap" name="choice">🥩 Beef Wrap</button>
    <button type="submit" value="fish wrap" name="choice">🐟 Fish Wrap</button>
    <button type="submit" value="fahita wrap" name="choice">🌮 Fahita Wrap</button>
    <button type="submit" value="drinks" name="choice">🍹 Drinks</button>
    <button type="submit" value="baladi ghada" name="choice">🍲 Baladi Ghada</button>
  </form>

  <div id="recipeContainer">
    <% if (locals.recipe) { %>
      <h2 id="recipeTitle">
        <%= recipe.name %>
      </h2>

      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li>
          <strong>Protein:</strong> <%= recipe.ingredients.protein.name %>, <%= recipe.ingredients.protein.preparation %>
        </li>
        <li>
          <strong>Salsa:</strong> <%= recipe.ingredients.salsa.name %> (<%= recipe.ingredients.salsa.spiciness %>)
        </li>
        <% recipe.ingredients.toppings.forEach(topping => { %>
          <li>
            <%= topping.quantity %> of <%= topping.name %> 
            (<%= topping.ingredients.join(", ") %>)
          </li>
        <% }) %>
      </ul>
    <% } else { %>
      <h2>Pick your order 👆</h2>
    <% } %>
  </div>

</body>

</html>
